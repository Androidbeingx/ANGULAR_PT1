import { PassrepeatDirective } from './passrepeat.directive';

describe('PassrepeatDirective', () => {
  it('should create an instance', () => {
    const directive = new PassrepeatDirective();
    expect(directive).toBeTruthy();
  });
});
